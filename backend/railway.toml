[build]
builder = "nixpacks"
nixpacksConfigPath = "nixpacks.toml"
# Note: buildCommand is now defined in nixpacks.toml

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

