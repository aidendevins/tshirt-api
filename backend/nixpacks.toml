[phases.setup]
# python311Full includes pip and other standard modules
nixPkgs = ["nodejs-18_x", "python311Full"]

# Install phase: npm install runs automatically by nixpacks when it detects package.json

# Build phase: Run our Python setup script after npm install
[phases.build]
cmds = ["bash build.sh"]

[start]
cmd = "npm start"

